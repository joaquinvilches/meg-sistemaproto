name: Build Multi-Platform

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:  # Permite ejecutar manualmente

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build Windows App
        run: npm run build:win

      - name: Upload Windows Installer
        uses: actions/upload-artifact@v4
        with:
          name: MEG-Sistema-Windows
          path: release/*.exe

  build-mac-intel:
    runs-on: macos-13  # Intel x64 architecture

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build Mac App (Intel x64)
        run: npm run build:mac:x64

      - name: Upload Mac Intel Installer
        uses: actions/upload-artifact@v4
        with:
          name: MEG-Sistema-Mac-Intel
          path: |
            release/*.dmg
            release/*.zip

  build-mac-arm:
    runs-on: macos-latest  # Apple Silicon (M1/M2/M3) arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build Mac App (Apple Silicon arm64)
        run: npm run build:mac:arm64

      - name: Upload Mac ARM Installer
        uses: actions/upload-artifact@v4
        with:
          name: MEG-Sistema-Mac-ARM
          path: |
            release/*.dmg
            release/*.zip

  # build-linux:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Build Linux App
  #       run: npm run build:linux

  #     - name: Upload Linux Installer
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: MEG-Sistema-Linux
  #         path: |
  #           release/*.AppImage
  #           release/*.deb

  create-release:
    needs: [build-windows, build-mac-intel, build-mac-arm]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            MEG-Sistema-Windows/*
            MEG-Sistema-Mac-Intel/*
            MEG-Sistema-Mac-ARM/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
